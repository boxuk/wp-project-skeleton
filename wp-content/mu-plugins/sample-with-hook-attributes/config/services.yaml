services:
    _defaults:
        autowire: true
        autoconfigure: true

    BoxUk\Mu\Plugins\SampleWithHookAttributes\SampleWthHookAttributes:
        public: true # If a service is being used directly (i.e. not being injected into another class) you have to mark it as public.
